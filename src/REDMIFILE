What is JSX, and why is it used?
JSX is a syntax extension for JavaScript that looks like HTML. It allows React to describe the UI structure in a declarative way, making code easier to read and write.

What is the difference between State and Props?
State is local, mutable data within a component. Props are read-only data passed from parent to child components. State changes can trigger re-render, while props cannot be modified by the child.

What is the useState hook, and how does it work?
useState is a React hook to manage state in functional components. It returns a state variable and a setter function to update it, causing the component to re-render on changes.

How can you share state between components in React?
State can be shared via lifting state up to a common parent, using props, context API, or state management libraries like Redux to pass data between components.

How is event handling done in React?
React handles events using camelCase syntax (onClick, onChange). Functions are passed as handlers, often defined inline or as component methods. Synthetic events normalize behavior across browsers.